# v0.3.0 Live Behavior Baseline

This baseline records representative live behavior at Git tag `v0.3.0`
(`a4a9012`) before the Agent Behavior Evaluation Loop was implemented. It is a
small comparison anchor, not a complete benchmark or release qualification.

## Environment

- Captured on 2026-08-27 with the canonical Compose stack on macOS and OrbStack.
- Model: `dgx_vllm::qwen3.6-35b-a3b-fp8`.
- Provider retries: `0` unless the workflow has a separate structured-output
  repair contract.
- Generated runtime artifacts remain ignored; this document preserves only the
  stable configuration and outcome needed for later comparisons.

## Agent Studio Chat Memory

- Test Center run: `f3b10432-9ab8-487e-81ef-e6ee465fabfd`.
- Prompt/persona: `chat_v20260516` and `chat_linxiaotang`.
- Embedding/fixture: `letta/letta-free` and `recent_user_chat_turns`.
- Result: `1/1` rounds passed in `235.461s` across seven conversation turns.
- Deterministic checks: zero forbidden self-disclosure hits, `human` memory
  changed, and all expected facts were captured (`张伟`, `Rocky`, and
  `哈士奇/Husky`).
- Signals: two memory tool calls out of two total tool calls.
- Cleanup: the disposable evaluation agent was archived and purged.
- Advisory judge: passed with score `100`; this did not determine the official
  result.

## Comment Lab

- Prompt/persona: `comment_v20260418` and `comment_linxiaotang`.
- Result: `1/1` attempts succeeded in `32.667s`.
- The response was a non-empty, publishable Simplified Chinese comment using
  the requested persona and model sampling profile.

## Label Lab

- Prompt/schema: `label_football_entities_v1` and
  `label_football_entity_groups_v1`.
- Result: the primary JSON Schema attempt succeeded with no validation errors.
- Extracted players: `Lionel Messi`, `Luis Suarez`.
- Extracted teams: `Inter Miami`, `Orlando City`.

## Interpretation

These probes establish that the three primary behavior paths were operational
at the tagged baseline. Future comparisons should use the typed Test Center
evaluation views for per-turn evidence and should continue to treat
deterministic checks as authoritative and LLM-judge output as advisory.
