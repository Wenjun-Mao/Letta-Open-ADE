# Paste into one ChatGPT Pro consultation after publication

I need an independent review of synthetic character-memory evidence. Use **only GitHub access** and this immutable documentation packet:

- Repository: `https://github.com/Wenjun-Mao/Letta-Open-ADE`
- Published documentation-only commit: `<PUBLISHED_DOC_REVIEW_COMMIT>`
- Packet directory at that exact commit: `<PUBLISHED_PACKET_TREE_URL>`

Confirm the full commit SHA you actually inspected. If the link or any file is inaccessible, say exactly what you could and could not inspect. Do not silently substitute the repository default branch, a moving branch, or prior knowledge. The retained implementation source revision named in the packet is provenance only; it is not guaranteed to be available on GitHub. The bounded contract excerpts in file 04 are the source information available for this review.

Please make an **answer-first, two-pass judgment**. In pass 1, read `README.md`, `01-answer-trajectories.json`, and `02-scope-lifecycle.json`. For each case and each target/follow-up turn, assess the anonymous candidate/delivered replies against the user's actual source testimony, its time and lifecycle, and source scope. Record your first-pass judgments before reading file 03 or 04. The nine matched target pairs are listed in the README. Assess `invalidated_ended` separately as confounded and `user_retraction` separately as one-sided. Do not read any H5 synthesis, previous verdict, or arm key, even if one is visible elsewhere in the repository. Do not infer an unobserved response for a missing label. Labels may become guessable from replies; state when that limits blindness.

In pass 2, read `03-expected-observed-deltas.json` and `04-contract-notes.md`. Revisit any judgments changed by actual admission, mutation, or the bounded contract. Distinguish a semantic error from an exact wording/source-span comparator mismatch. Distinguish failure to recall an eligible and admitted source from a source excluded by scope or absent from admission. Distinguish an unsupported referent from a valid inference with stated uncertainty. Keep earlier historical testimony separate from claims about the user's current state. Treat a follow-up as a trajectory dependent on its own preceding target; do not count it as an independent paired sample. The frozen fixture is a hypothesis, not an oracle, and a passing validator is not proof of semantic correctness.

For every one of the nine matched pairs, give a concise evidence-backed judgment on factual accuracy, source attribution, temporal accuracy, unwanted callback, and conversational naturalness for each anonymous label. Cite the packet's case ID, turn, source exchange ID or lifecycle entry, and the decisive short quote. Identify concrete errors, acceptable variants, uncertainty, and the strongest competing interpretation. For the three paired follow-up trajectories, say whether the earlier turn changed what the follow-up could reasonably do. Give a separate assessment of the confounded and one-sided cases without incorporating them into a paired conclusion.

Then say whether the evidence justifies any change to the **general memory contract**. If yes, propose the smallest general amendment and why it belongs at that contract layer; describe an alternative explanation and what additional evidence would discriminate. If no amendment is justified, say so. Avoid case-specific phrase rules, speculative privacy features, new architecture, or validators that purport to prove semantics. Do not propose a code change before completing the answer assessment. Be explicit where this packet is insufficient for a conclusion.

Return a table by case/turn/label plus a short synthesis of paired patterns, mutation interpretation, and any contract recommendation. Separate observed facts, inference, and recommendation. This is external AI advice for a later human review, not independent human acceptance or production qualification.
