[project]
name = "model-catalog-contracts"
version = "0.3.0"
description = "Stable model catalog and provider probe artifact contracts."
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/model_catalog_contracts"]
